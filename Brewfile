# System dependencies for macOS development and deployment.
# Python dependencies remain locked separately in pyproject.toml / uv.lock.

# Local, deterministic OCR used by the FactSet Earnings Insight Sector decoder.
brew "tesseract"
