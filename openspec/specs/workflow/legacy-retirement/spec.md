# workflow/legacy-retirement Specification

## Purpose

为兼容迁移立一套可执行的退出机制。设计文档 §12.4 规定所有 schema 变更只做 additive、不删旧表旧列、在新写路径稳定前保留旧读入口——这条原则本身正确，但若无登记与判据，后续阶段将无法回答「哪套旧实现已可退出、退出条件是什么、何时下线」，兼容层会无限期堆积，最终新旧两条路径长期并存，审计真相与写入边界同时被稀释。本能力把「墓碑登记 → fail-closed 读取门 → 两段式清除」固定为退出旧实现的唯一合法路径，并要求每个阶段在开工时声明其退出面。机制沿用结构化数据层已有的 `retired_sources` 墓碑模式，不另造一套。

## Requirements

### Requirement: 旧实现的退出必须以墓碑登记为前提

任何旧实现（旧表、旧列、旧写入路径、旧读入口）的退出 SHALL 先在登记处留下墓碑记录，包含其标识、所属能力域、被谁替代、目标相位与退出条件。同一标识 SHALL NOT 同时存在于「在用」与「已退役」两处；两处并存时配置加载 SHALL 失败并指出冲突标识，SHALL NOT 以任一处的记录静默取胜。

#### Scenario: 登记一处新墓碑

- **WHEN** 某个旧实现的退出条件已满足并被登记为退役
- **THEN** 登记处 SHALL 出现该标识的墓碑记录
- **AND** 该标识 SHALL 不再出现在在用清册中

#### Scenario: 同一标识两处并存

- **WHEN** 一个标识同时存在于在用清册与已退役登记中
- **THEN** 配置加载 SHALL 失败并指出该标识
- **AND** 系统 SHALL NOT 选择其中一处继续运行

### Requirement: 命中墓碑的读取必须 fail-closed

当读取请求指向已登记退役的旧实现时，系统 SHALL 返回显式的退役原因，SHALL NOT 静默回退到替代实现的近似结果，SHALL NOT 返回空集或默认值掩盖退役事实。退役原因 SHALL 可被调用方与审计记录区分于「不存在」与「无数据」两种情形。

#### Scenario: 读取已退役的旧实现

- **WHEN** 一个消费方请求读取已退役的旧表或旧接口
- **THEN** 系统 SHALL 返回说明该实现已退役的原因码
- **AND** 该结果 SHALL NOT 与「未找到」或「无数据」混淆

#### Scenario: 旧写入路径在退役后仍被调用

- **WHEN** 某条旧写入路径在登记退役之后仍被触发
- **THEN** 该次调用 SHALL 判定为失败并指出已退役
- **AND** 系统 SHALL NOT 把该次写入落到替代实现上以求成功

### Requirement: 旧实现的物理清除必须两段式

清除旧实现或其承载数据 SHALL 分两段：默认执行只读干跑并报告将被影响的范围，仅当显式确认时才执行物理删除。清除 SHALL 支持声明数据已在别处留存，并 SHALL 把清除动作与备注写入可审计的记录。系统 SHALL NOT 在未确认的情况下执行不可逆删除。

#### Scenario: 未确认时的清除请求

- **WHEN** 发起一次旧实现清除而未给出显式确认
- **THEN** 系统 SHALL 只做只读干跑并列出受影响范围
- **AND** 实际数据 SHALL NOT 被修改或删除

#### Scenario: 确认后的清除留下审计记录

- **WHEN** 一个清除请求带显式确认被执行
- **THEN** 清除动作、范围与备注 SHALL 被写入审计记录
- **AND** 若声明数据已在别处留存，该声明 SHALL 一并留痕

### Requirement: 每个阶段必须登记其待退旧实现与退出条件

每个迁移阶段 SHALL 在其规划产物中登记该阶段涉及的全部待退旧实现，并逐项写明：替代实现、退出条件、依赖消费方清零的判据、以及退出未完成时的处理方式。对退出条件尚不满足的项，SHALL 明确标注为「只登记不退出」并说明所缺条件，SHALL NOT 省略登记。

#### Scenario: 阶段涉及旧实现但尚未可退

- **WHEN** 某阶段的替代实现已就绪，但其旧实现的依赖消费方尚未清零
- **THEN** 该旧实现 SHALL 被登记并标注退出条件未满足
- **AND** 登记 SHALL 说明所缺条件与判据

#### Scenario: 阶段遗漏待退项

- **WHEN** 某阶段引入了替代实现但未登记被其替代的旧实现
- **THEN** 该阶段的规划产物 SHALL 判定为不完整
- **AND** 缺失项 SHALL 被指出
